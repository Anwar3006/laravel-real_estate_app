/**
 *
 * @returns a PropertyListing through Inertia from Laravel to React
 * @author Agent(access level is Agent)
 */
const Show = () => {
  return <div>Show Single Listing</div>;
};

export default Show;
